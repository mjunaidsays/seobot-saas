'use client';

import { useEffect } from 'react';
import { Crisp } from 'crisp-sdk-web';

const CrispChat = () => {
  useEffect(() => {
    // add Crisp website ID here
    // Crisp.configure('crisp_id');
  });

  return null;
};

export default CrispChat;
